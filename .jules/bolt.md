## 2024-05-22 - Vitest Configuration Missing
**Learning:** The project uses `vitest` but fails to run tests due to missing alias configuration for `@/`.
**Action:** When running tests, either temporary create `vitest.config.ts` or fix the root cause if permitted.
