## 2025-02-20 - Icon-Only Button Accessibility
**Learning:** The `DocumentsTable` component uses icon-only buttons for group expansion without accessible labels.
**Action:** Always verify icon-only buttons have `aria-label` or `aria-labelledby`. Used `aria-expanded` for toggle states.
